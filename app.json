{
    "env": {
        "NODE_ENVR": "production-environment",
        "SECRET_TOKEN": {
          "description": "A secret key for verifying the integrity of signed cookies.",
          "generator": "secret"
        },
        "WEB_CONCURRENCY": {
          "description": "The number of processes to run.",
          "value": "5"
        }
    },
    "environments": {
      "review": {
        "scripts": {
          "postdeploy": "set NODE_ENV=local&& node src/index.js"
        }
      }
    }
}